import 'package:flutter/material.dart';

class ConversationPafe extends StatelessWidget {
  const ConversationPafe({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("WelcomeToCOnversatioon",),),
      
    );
  }
}