import 'package:flutter/material.dart';

class StartNewMessageScreen extends StatelessWidget {
  const StartNewMessageScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Hello New Message"),
    );
  }
}